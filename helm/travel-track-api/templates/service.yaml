apiVersion: v1
kind: Service
metadata:
  name: {{ include "travel-track-api.fullname" . }}
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "travel-track-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "travel-track-api.selectorLabels" . | nindent 4 }}
